version: '2'
services:
  smartcontracts:
    build: ./smartcontracts/
    volumes:
      - contracts:/home/node/smartcontract/build/
  client:
    build: ./client/
    ports:
      - 3000:3000
    volumes:
      - contracts:/home/node/client/src/contracts/
      - ./client/src/:/home/node/client/src/
    depends_on:
      - smartcontracts
  api_db:
    build: ./api_db/
    ports:
      - 8080:8080
    depends_on:
      - redis
  redis:
    image: redis:alpine
    command: redis-server --requirepass 12345678 --appendonly yes
    ports:
      - 6379:6379
    volumes:
      - data:/data
volumes:
  data:
  contracts: